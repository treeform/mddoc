# Package

version       = "0.0.1"
author        = "treeform"
description   = "Generate Nim API docs in markdown for GitHub's README.md files."
license       = "MIT"
srcDir        = "src"

# Dependencies

requires "nim >= 1.0.0"
