# Package

version       = "0.0.4"
author        = "treeform"
description   = "Generate Nim API docs in markdown for GitHub's README.md files."
license       = "MIT"
srcDir        = "src"

bin = @["mddoc"]

# Dependencies

requires "nim >= 1.0.0"
